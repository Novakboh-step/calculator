class Calculator:
    def add(self, a, b):
        mes = f"{a} + {b} = {a + b}"
        print(mes)
        return a + b

    def subtract(self, a, b):
        mes = f"{a} - {b} = {a - b}"
        print(mes)
        return a - b